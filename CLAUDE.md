# deploymate

## Publishing

The package is published to npm as [`deploymate-cli`](https://www.npmjs.com/package/deploymate-cli) and run by users via `npx deploymate-cli`.

**Do not bump the version in `package.json` for bug fixes or small changes** unless explicitly asked to cut a release.
